@version: 3.29
@include "scl.conf"
source s_network {
  syslog(transport({{ SYSLOG_PROTO}}) port({{ SYSLOG_PORT }}));
};

destination d_loki {
  syslog("promtail" transport("tcp") port(1514));
};

log {
  source(s_network);
  destination(d_loki);
};
